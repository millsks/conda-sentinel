- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-identity-s08-resolution-finds-its-mappings.md`
  summary: The bounded second call in `feedstock.py` is retried by the mounted transport, so its soft-limit arithmetic (and `test_feedstock.py:349-363`) understates the ceiling by a full retried call.
  evidence: `RequestsTransport` mounts `Retry(total=retries)` from the collector's own `retries`; `worst_case_call_seconds(timeout, retries)` applies to every fetch through it, not only the base's. S08 fixed its own instance of the same misreading.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-identity-s08-resolution-finds-its-mappings.md`
  summary: A collector's second fetch inside `translate` is neither cached, conditional nor charged to the allowance, and now three collectors make one (`feedstock`, `source_release`, `resolve_identity`); the resolver's un-metered host is pypi.org, the busier of its two.
  evidence: `feedstock.py:1726-1728` already records the gap as deferred; the base has one cache/limiter seam keyed on the single `source`. A base-level "bounded second source" would close it for all three.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-identity-s08-resolution-finds-its-mappings.md`
  summary: The resolution written by `record_resolution` and the snapshot row inserted by `_write_evidence` commit as two transactions, so a database failure between them leaves identity mutated with no evidence row.
  evidence: `collection_run` deliberately opens no transaction; `translate` cannot insert its own row. Closing it needs a base seam that lets a collector run a callable inside `_write_evidence`'s block.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-identity-s08-resolution-finds-its-mappings.md`
  summary: Automated resolution never records `not_applicable` for the release-ecosystem mapping, so a conda-only package (`_libgcc_mutex`) is `not_found` on PyPI rather than "not a Python package", and `python_readiness`'s `not_applicable` branch is unreachable through automation.
  evidence: Deciding "not applicable" needs a package-type source (conda-forge's recipe `noarch`/language, or the inventory), which no collector reads yet; the resolver has only a name.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-identity-s08-resolution-finds-its-mappings.md`
  summary: The collector name `resolve_identity` is also a work-type vocabulary value (`policies/outcomes.py`, `surface/tone.py:143`), so the two share one derived label and tone on different screens.
  evidence: Both vocabularies are keyed by bare value and `test_no_label_carries_the_slug_separator` sweeps them into one set, so the collision is invisible to the tests; renaming either is a vocabulary change with its own migration.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-platform-s08-the-demo-asserts-nothing-it-never-observed.md`
  summary: A name-keyed resolution reads whatever PyPI project shares a conda package's name, so `nodejs`, `ffmpeg` and `ripgrep` (unrelated PyPI projects exist) can be recorded with an unrelated repository and `pkg:pypi/<name>`.
  evidence: The resolver asks PyPI by the canonical name and has no package-type or cross-ecosystem source to refuse a coincidence; the demo roster's native packages make it visible. Closing it is the cross-ecosystem mapping (`CROSS_ECOSYSTEM`, today always `not_found`) or a package-type source, neither of which a seeder change can supply.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s03-the-inventory-is-a-governed-table.md`
  summary: A package retired from the inventory keeps its identity-queue and workflow items; nothing downstream reads inventory absence. RESOLVED into CPM-OPERATE-S11 on 2026-09-13.
  evidence: `collectors/selection.py:132-138` sorts a `not_found` latest snapshot last for want of breadth but does not exclude it; `workflow/opening.py:126` opens identity-review items for every `unmapped` package; `core/rollup.py:166` rolls up every package. The epic's S03 line "leaves the queues" described behaviour that did not exist; deciding what an absent package should do in each queue is a policy story.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s02-ingest-sweep-and-run-are-commands.md`
  summary: The demo seeder's `_shipped_policy_version` picked "newest" by lexicographic string sort. RESOLVED by CPM-OPERATE-S03 on 2026-09-13 (`policies.parameters.newest_recorded_version()`); re-added here because the ledger is append-only and the entry had been removed rather than marked.
  evidence: `policies/parameters.py` now owns `version_key`/`recorded_versions`/`newest_recorded_version`; both the `run_policy` command and the seeder import them.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s05-an-authenticated-github-allowance.md`
  summary: With `CPM_GITHUB_TOKEN` set the feedstock collector still declares one allowance for both branches -- the authenticated *search* number, 30/min -- so mapped-branch packages (repository + recipe, GitHub's 5,000/h core pool) are throttled to about 450 an hour when the remote budget would allow about 1,250.
  evidence: `collectors/feedstock.py` `__init__` selects `AUTHENTICATED_SEARCH_ALLOWANCE`; the base charges `self._rate_limit` once in `core/collection.py` before `source_for` decides the branch, and `window_key` has no per-pool dimension. Closing it needs the base to let a collector declare an allowance per locator, or a branch-aware charge.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s05-an-authenticated-github-allowance.md`
  summary: A token that appears or disappears mid-window is judged under the new allowance for the rest of that window, because the rate-limit key names the collector and the window index only; a counter at 58 of 60 becomes 58 of 5,000 without turning over. Documented in `operations.md`, not changed.
  evidence: `core/rate_limit.py` `window_key` is `{prefix}:{collector}:{window}` and the limit is a comparison argument, not part of the key; each task constructs a fresh collector reading the setting live, so the switch happens between tasks. Harmless in the raising direction and at most one window of over-grant in the lowering one.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-currency-s01-upstream-release-evidence.md`
  summary: The declared allowance cannot sweep the inventory (sixty unauthenticated requests an hour, fifteen packages an hour against CPM-NFR-1's ten thousand); raising it means authenticating. RESOLVED by CPM-OPERATE-S05 on 2026-09-13 (`CPM_GITHUB_TOKEN`, `collectors/github.py`, the instance-level `AUTHENTICATED_CORE_ALLOWANCE`); recorded here because the entry lives in the story's own frontmatter and the ledger is append-only.
  evidence: `collectors/source_release.py` `__init__` declares the bearer and 3,200 core requests an hour when the setting is present; `docs/conda-sentinel/operations.md` carries the sweep arithmetic (about 12.5 hours for ten thousand packages against a two-day target). The uncharged tag-fallback call the entry also names stays deferred (the `CPM-IDENTITY-S08` entry above).
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-currency-s03-feedstock-evidence.md`
  summary: The declared allowance is GitHub's search allowance (ten a minute unauthenticated) and nothing had measured it against a sweep; raising it means authenticating. RESOLVED by CPM-OPERATE-S05 on 2026-09-13 (`CPM_GITHUB_TOKEN`, the instance-level `AUTHENTICATED_SEARCH_ALLOWANCE` at thirty a minute); recorded here because the entry lives in the story's own frontmatter and the ledger is append-only.
  evidence: `collectors/feedstock.py` `__init__` declares the bearer and the authenticated search allowance; the arithmetic in `docs/conda-sentinel/operations.md` (about 67 hours unauthenticated, about 22 hours authenticated, against a fourteen-day target) corrects the entry's "does not fit a weekly cadence with any margin". The single-allowance-for-both-branches limitation stays deferred (the CPM-OPERATE-S05 entry above).
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s05-an-authenticated-github-allowance.md`
  summary: Correction to the two CPM-OPERATE-S05 entries above, after review -- the authenticated core allowance is 3,200 an hour (the 5,000 pool less feedstock's worst-case 1,800 core spend), not 5,000, and a token changes only at a worker restart because the setting is evaluated once per process; the mid-window entry's mechanism is therefore "a restart with a new token", and its 58-of-5,000 example reads 58-of-3,200. Both entries stay deferred as written.
  evidence: `collectors/github.py` `AUTHENTICATED_CORE_ALLOWANCE` carries the arithmetic; `config/settings/base.py` reads `CPM_GITHUB_TOKEN` at import; `docs/conda-sentinel/operations.md` documents the restart-and-window behaviour.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s07-ninety-days-of-evidence-purged-nightly.md`
  summary: A non-integer `CPM_EVIDENCE_RETENTION_DAYS` in the *environment* (`abc`) is refused at settings import by django-environ's `ValueError`, which names the value and not the setting; the boot refusal that names the setting (`CollectorsConfig.ready()`) reaches only a value that parsed as an integer and is below one, or a settings module that assigned the wrong type by hand.
  evidence: `config/settings/base.py` reads it with `env.int(...)` as the story specifies and carries no `try`/`raise` anywhere; `tests/unit/test_settings.py::test_a_retention_that_is_not_a_number_refuses_the_settings_module` pins the `ValueError`. Naming the setting on that path means either a guarded read in `base.py` (a first `raise` in a module that has none) or reading the variable as text and parsing it in the hook, and neither is this story's to decide.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s07-ninety-days-of-evidence-purged-nightly.md`
  summary: The purge's floor rule protects only evidence rows a *policy run* read at its cut-off; the observation-window check in `core/collection.py` reads the newest `collection_runs` row per package and collector, and after a purge of ledger rows older than the retention a collector that has not run for longer than the retention sees no previous run at all -- harmless (the window is hours or days, so an absent row means "collect"), but a second reader of the ledger the rule does not name.
  evidence: `core/retention.py` step 3 deletes `collection_runs` rows by `finished_at`/`started_at` alone; `core/collection.py`'s window query and `surface/coverage.py` read only recent rows, and every declared observation window is far shorter than `MINIMUM_RETENTION_DAYS` would allow a deployment to set. A retention of one day against a weekly window would purge the row the window check reads; nothing refuses that combination at boot.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s08-a-collector-re-run-for-one-package-from-the-page.md`
  summary: The in-flight refusal reads the run ledger, so a second "Collect now" press in the gap between the publish and the worker opening the first task's ledger row (tens of milliseconds on the local stack; longer behind a queue) is admitted and enqueues a second forced set of runs behind the first.
  evidence: `collectors/recollection.py` `_require_nothing_in_flight` filters `CollectionRun.objects.unfinished()` by `started_at`; nothing between the audit row and the worker's `collection_run` recorder marks the package as pending. The live check on 2026-09-14 met the 409 at a 400 ms second press and a 303 at a 4 s one only because the worker had already finalised the first set; the sweep's overlap guard has the same shape. Closing it means reading `package_recollections` as well (a recollection newer than the newest run on the package, within the window, counts as in flight), which changes what "in flight" means to the page.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s08-a-collector-re-run-for-one-package-from-the-page.md`
  summary: `PackageRecollectView` is exempt from `ATOMIC_REQUESTS` (`transaction.non_atomic_requests`) so the service's `on_commit` publish runs inside the request and the receipt can name what the broker refused; the second product view to take the exemption after the health probes, and the first that writes.
  evidence: `surface/views.py` `PackageRecollectView` docstring; `tests/integration/django_apps/test_recollection.py::test_the_view_is_exempt_from_atomic_requests_for_every_alias_that_declares_it`. The service's own `atomic()` is the only transaction the request opens, so nothing is lost; recorded because `test_health_views.py` pins the exemption for the probes and nothing yet pins how many views may take it.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s08-a-collector-re-run-for-one-package-from-the-page.md`
  summary: The queue-latency gap in the in-flight guard (the first CPM-OPERATE-S08 entry above) is RESOLVED by the review patch on 2026-09-14: in flight now also means a `package_recollections` row within the window that the ledger has not answered in full, the check and the insert run with the package row locked (`select_for_update`), the window is twice `CELERY_TASK_TIME_LIMIT` read at call time, and each published task carries `expires` set to it.
  evidence: `collectors/recollection.py` `pending_recollection`, `_lock`, `in_flight_window`, `_publish`; `tests/integration/django_apps/test_recollection.py` (`..._refuses_a_second_press_naming_the_press`, `..._is_answered_and_a_new_press_is_allowed`, `..._locked_before_the_in_flight_check_and_the_insert`). What the suite cannot show is two real threads serialising on the row: the suite runs on SQLite, which has no `FOR UPDATE` and on which Django omits the clause, so the lock's order and target are asserted and the serialisation itself is PostgreSQL's to prove.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s08-a-collector-re-run-for-one-package-from-the-page.md`
  summary: `core/0005_grant_identity_override` and `core/0011_grant_inventory_change` put `app_config.models_module` back to `None` in their `finally`, so a test that runs their `forward` against the live registry (`test_role_groups.py` does) leaves the `identity` and `collectors` app configs with no models module for the rest of the process, after which `post_migrate` creates no content type and no permission for either app -- invisible until a later `transaction=True` case flushes the database and needs them back.
  evidence: Found by `test_recollection.py`'s transactional cases failing only after `test_role_groups.py` in a full run; `core/0013_grant_recollect` puts back what it found (`getattr(app_config, "models_module", None)`), and `test_recollection.py`'s `_provisioned()` repairs the older two's leak before it re-provisions. Editing `0005`/`0011` is one line each and behaviour-preserving on the historical stub, but they are earlier stories' migrations and were left as they are.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s09-an-operator-digest.md`
  summary: Older digests have no detail route. The Digests page shows the newest row in full -- text, figures per collector and overall, deliveries, trace id -- and the thirty before it only as a list (date, changed, delivery states); an operator wanting yesterday's text reads the mail or the webhook it was delivered to, or the row through the admin.
  evidence: `surface/views.py` `DigestView` and `surface/urls.py` declare one route, `digests/`; `surface/digest.py` `newest_and_history` reads one slice and projects only the newest; `templates/conda_sentinel/digest.html` lists `history` without links. A `digests/<int:pk>/` detail view over the same projection is the obvious shape and was left for a story that asks for it.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s10-the-evidence-tables-at-ten-thousand-packages.md`
  summary: The purge's selection for `vulnerability_findings` sequentially scans `kev_findings` and `vulnerability_findings` once per batch -- 5627.893 ms and `shared hit=18170811 read=234729` for the first batch's selection, 25.84 s of the 71.48 s nightly purge at ten thousand packages -- because it excludes rows a surviving `kev_findings` row cites, "surviving" is the complement of kev's own floor rule, and kev's reader key `(package, vulnerability_finding__advisory_id)` holds a column of the joined table, which the planner answers by hashing the whole `kev LEFT JOIN vuln`. Inside the hour with margin; recorded, not fixed.
  evidence: `core/retention.py` `_keyed` annotates the joined key with `Coalesce(F("vulnerability_finding__advisory_id"), "")` and `floor_removable_evidence` correlates on it; `_surviving_citers` nests `removable_evidence(kev)` inside vuln's selection. No index on `kev_findings` can carry the joined column, so the story's index rule demanded none; closing it means keying kev by a column it owns (denormalising the advisory onto `kev_findings`, or keying by `vulnerability_finding_id` with a rule that a superseding advisory row supersedes its cross-reference), which is a change to `CPM-OPERATE-S07`'s floor-rule semantics. Named in the spike's `RECORDED_SCANS`; the plan is verbatim in the story's `## Measurement`.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s10-the-evidence-tables-at-ten-thousand-packages.md`
  summary: The rollup's per-package reads are N+1 by design (`CPM-AD-23`, one transaction per package) and their measured budget at ten thousand packages is small: 46 statements and 0.424 ms of database execution for one package, `x 10,000 packages = 4.2 s of database execution` per policy run, every statement one probe of its `(package, -observed_at)` index on 4-5 shared buffers. Recorded so the set-based rewrite `operations.md` ("What a run costs") already defers is weighed against 4.2 s of reads, not against a guess; not changed.
  evidence: `tests/spikes/spike_evidence_scale.py::test_the_rollup_reads_for_one_package` and the printed `rollup read budget` line; `core/policy_run.py:388-459` is the loop. The round trips, not the reads, are what a rewrite would save.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s10-the-evidence-tables-at-ten-thousand-packages.md`
  summary: The spike's row-count and plan-shape assertions are the regression check, but nothing in the gate runs them: a change to an evidence index, the floor rule or the purge's batching is caught only when somebody runs `pixi run spike-scale` (Docker, about four and a half minutes). The gate cannot carry a twenty-five-million-row seed; the obligation is documented in `operations.md` ("Measuring at scale") and in the spike's docstring, and is a documented obligation rather than an automated one, on the terms `docs/accelerator/development.md` states for the dependency-fitness half of FR-50.
  evidence: `tests/unit/test_gate_contract.py::test_the_gate_cannot_collect_the_storage_spike` keeps every `spike_*.py` out of `pytest tests/`; `scripts/spike-scale.sh` is the only thing that names the module.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s10-the-evidence-tables-at-ten-thousand-packages.md`
  summary: Correction to the three CPM-OPERATE-S10 entries above after review loop 1 -- their numbers came from a superseded run (package-major seed, two derived runs); the recorded run seeds day-major with every run's derived rows and reads: `vulnerability_findings` first-batch selection 11245.349 ms and `shared hit=15083741 read=3129288, temp read=29074 written=29340`, 21.43 s of an 84.96 s purge; `prune_evidence --dry-run`'s unbatched count of that selection 15635.661 ms; the rollup budget 48 statements and 0.391 ms for one package, `x 10,000 packages = 3.9 s`. The seeder is now smoke-run by the gate (`tests/integration/django_apps/test_evidence_scale_seed.py`); the plan-shape and purge assertions remain spike-only.
  evidence: `.spike-runs/20260914T100410Z.log`, verbatim in the story's `## Measurement`. The first-batch figures carry `EXPLAIN ANALYZE`'s per-node instrumentation, a cache the seed and `VACUUM (ANALYZE)` just wrote (the `read=` counts) and a hash that spills `work_mem = 4MB` (`temp`), so per-batch × batches is an upper bound on a table's purge, not its total; the purge's own per-table wall clock is the total.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s10-the-evidence-tables-at-ten-thousand-packages.md`
  summary: `removable_evidence` hashes the citing side of its anti-join for the three evidence tables the derived rows cite through a nullable foreign key: `Parallel Seq Scan` on `package_currency`, `package_remediation` and `package_feedstock_presence` (900,000 rows each at steady state) once per batch of `source_release_snapshots`, `pypi_release_snapshots` and `feedstock_snapshots` -- 387.720, 297.728 and 1747.635 ms for the first batch against 110-111 ms for the floor rule alone, and 10.56 s of the night for `feedstock_snapshots`. The foreign key's own index exists and the planner declines it at this ratio (a thousand candidates against nine hundred thousand citing rows). Recorded, not fixed: it is a planner choice, not a missing index.
  evidence: `core/retention.py` `_surviving_citers` builds one `NOT EXISTS` per reverse relation, filtered to surviving runs (every run but one); the plans in the story show `Parallel Hash Right Anti Join` with the derived table hashed. Shapes that might change the estimate -- a partial index on the citing column where not null, or excluding the citer check for a derived table whose column is null for the batch -- change what the selection asks and are `CPM-OPERATE-S07`'s to weigh. The spike admits sequential scans on the citing side of these selections only; a scan of the selected table or the ledger still fails.
- source_spec: `_bmad-output/implementation-artifacts/stories/cpm-operate-s10-the-evidence-tables-at-ten-thousand-packages.md`
  summary: The purge's batch selection is the keyset `_batches` chooses -- `id > watermark ORDER BY id LIMIT n` with the retention cut-off as a filter -- so its cost depends on where the oldest rows sit in the primary key. On the day-major seed the walk meets day 0 first (`rows=1010` read for 1,000 selected) and the bare `observed_at` index serves only `_older` and the digest; rows older than the cut-off that are not lowest in the key -- inserted late, or left by a purge that lagged its nights -- are filtered past one page at a time. A keyset on `(observed_at, id)` would make the walk independent of insertion order and let the time index serve the selection; it changes a product query's shape and is `CPM-OPERATE-S07`'s to measure, with `pixi run spike-scale` seeded out of order.
  evidence: `core/retention.py` `_batches` (`filter(pk__gt=watermark).order_by("pk")`); the story's `purge.floor_removable_evidence first batch` plans (`Index Scan using <table>_pkey`, `Filter: observed_at < cutoff`), and the fixed per-batch cost they show: 89,000 `SubPlan 2` probes per batch of a thousand, one per candidate per surviving policy run.
